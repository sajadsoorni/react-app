import { useEffect } from 'react';
import ProductList from './components/ProductList';

function App() {
  // afterRender
  useEffect(() => {});

  return (
    <div>
      <ProductList />
    </div>
  );
}

export default App;
